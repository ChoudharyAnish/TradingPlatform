# Example research notebook placeholder

Record for every experiment:
- dataset_version, date range, features, target, model, hyperparameters
- train/valid/test windows, transaction cost assumptions, results, model version

Never overwrite prior experiment folders — write a new timestamped directory under research/experiments/.
